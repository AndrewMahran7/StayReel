// components/BannerAdView.tsx
// Ads are disabled. This component renders nothing and exists only as a
// safe no-op stub so stale imports elsewhere don't cause build failures.

import React from 'react';

interface Props {
  style?: object;
}

export function BannerAdView(_props: Props) {
  return null;
}
