// components/BannerAdView.tsx
// Ads removed — returns null until a native build is set up.

import React from 'react';

interface Props {
  style?: object;
}

export function BannerAdView(_props: Props) {
  return null;
}
